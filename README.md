# 🚀 Remote Server Control (RSC)

Простая и мощная веб-панель для удаленного управления вашим ПК или сервером.  
**Функции:** Терминал, Файловый менеджер, Диспетчер задач, Мониторинг ресурсов.

---

## 🛠️ Установка
Перед первым запуском установите необходимые библиотеки:
```bash
pip install -r requirements.txt
```

---

## 🪟 Windows

Просто запустите нужный файл двойным кликом:

| Файл | Описание |
| :--- | :--- |
| **`run.bat`** | Обычный запуск (HTTP). |
| **`run_https.bat`** | **Рекомендуется для мобильных.** Запуск с шифрованием (HTTPS). |

---

## 🐧 Linux

1. Сделайте скрипты исполняемыми (один раз):
   ```bash
   chmod +x *.sh
   ```

2. Запустите нужный скрипт:

| Команда | Описание |
| :--- | :--- |
| **`./run.sh`** | Обычный запуск (HTTP). |
| **`./run_https.sh`** | **Рекомендуется для мобильных.** Запуск с шифрованием (HTTPS). |

---

## 📱 Как подключиться с телефона

1. Убедитесь, что телефон и компьютер находятся в **одной Wi-Fi сети**.
2. Запустите сервер (желательно в режиме **HTTPS**, так как современные браузеры блокируют HTTP).
3. Откройте панель на компьютере (`https://localhost:5000`).
4. Нажмите на зеленую иконку **QR-кода** в правом верхнем углу.
5. Отсканируйте код камерой телефона.

> **Важно:** При использовании HTTPS браузер покажет предупреждение "Подключение не защищено" (так как сертификат самоподписанный).  
> Нажмите **"Подробнее"** (Advanced) -> **"Перейти на сайт"** (Proceed).
